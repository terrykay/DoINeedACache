<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/charcoal</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorControlNormal">@color/white</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="titleBarTextColour">@color/white</item>
        <item name="android:textColor">@color/white</item>
        <item name="calendarTodayColour">@color/green</item>
        <item name="calendarPageColour">@color/charcoal</item>
        <item name="calendarHeaderColour">@color/black</item>
        <item name="calendarTextColour">@color/white</item>
    </style>
</resources>